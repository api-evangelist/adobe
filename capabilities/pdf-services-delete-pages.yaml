naftiko: 1.0.0-alpha2
info:
  label: Adobe PDF Services API — Delete Pages
  description: 'Adobe PDF Services API — Delete Pages. 1 operations. Lead operation: Adobe Delete Pages From a Pdf. Self-contained
    Naftiko capability covering one Adobe business surface.'
  tags:
  - Adobe
  - Delete Pages
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADOBE_API_KEY: ADOBE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pdf-services-delete-pages
    baseUri: https://pdf-services-ue1.adobe.io
    description: Adobe PDF Services API — Delete Pages business capability. Self-contained, no shared references.
    resources:
    - name: operation-deletepages
      path: /operation/deletepages
      operations:
      - name: deletepages
        method: POST
        description: Adobe Delete Pages From a Pdf
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ADOBE_API_KEY}}'
  exposes:
  - type: rest
    namespace: pdf-services-delete-pages-rest
    port: 8080
    description: REST adapter for Adobe PDF Services API — Delete Pages. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/operation/deletepages
      name: operation-deletepages
      description: REST surface for operation-deletepages.
      operations:
      - method: POST
        name: deletepages
        description: Adobe Delete Pages From a Pdf
        call: pdf-services-delete-pages.deletepages
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pdf-services-delete-pages-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adobe PDF Services API — Delete Pages. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adobe-delete-pages-pdf
      description: Adobe Delete Pages From a Pdf
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: pdf-services-delete-pages.deletepages
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
