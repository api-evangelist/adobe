naftiko: 1.0.0-alpha2
info:
  label: Adobe PDF Services API — Extract PDF
  description: 'Adobe PDF Services API — Extract PDF. 1 operations. Lead operation: Adobe Extract Content From a Pdf. Self-contained
    Naftiko capability covering one Adobe business surface.'
  tags:
  - Adobe
  - Extract PDF
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADOBE_API_KEY: ADOBE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pdf-services-extract-pdf
    baseUri: https://pdf-services-ue1.adobe.io
    description: Adobe PDF Services API — Extract PDF business capability. Self-contained, no shared references.
    resources:
    - name: operation-extractpdf
      path: /operation/extractpdf
      operations:
      - name: extractpdf
        method: POST
        description: Adobe Extract Content From a Pdf
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ADOBE_API_KEY}}'
  exposes:
  - type: rest
    namespace: pdf-services-extract-pdf-rest
    port: 8080
    description: REST adapter for Adobe PDF Services API — Extract PDF. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/operation/extractpdf
      name: operation-extractpdf
      description: REST surface for operation-extractpdf.
      operations:
      - method: POST
        name: extractpdf
        description: Adobe Extract Content From a Pdf
        call: pdf-services-extract-pdf.extractpdf
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pdf-services-extract-pdf-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adobe PDF Services API — Extract PDF. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adobe-extract-content-pdf
      description: Adobe Extract Content From a Pdf
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: pdf-services-extract-pdf.extractpdf
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
