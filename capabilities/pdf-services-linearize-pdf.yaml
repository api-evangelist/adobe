naftiko: 1.0.0-alpha2
info:
  label: Adobe PDF Services API — Linearize PDF
  description: 'Adobe PDF Services API — Linearize PDF. 1 operations. Lead operation: Adobe Linearize a Pdf. Self-contained
    Naftiko capability covering one Adobe business surface.'
  tags:
  - Adobe
  - Linearize PDF
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADOBE_API_KEY: ADOBE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pdf-services-linearize-pdf
    baseUri: https://pdf-services-ue1.adobe.io
    description: Adobe PDF Services API — Linearize PDF business capability. Self-contained, no shared references.
    resources:
    - name: operation-linearizepdf
      path: /operation/linearizepdf
      operations:
      - name: linearizepdf
        method: POST
        description: Adobe Linearize a Pdf
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ADOBE_API_KEY}}'
  exposes:
  - type: rest
    namespace: pdf-services-linearize-pdf-rest
    port: 8080
    description: REST adapter for Adobe PDF Services API — Linearize PDF. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/operation/linearizepdf
      name: operation-linearizepdf
      description: REST surface for operation-linearizepdf.
      operations:
      - method: POST
        name: linearizepdf
        description: Adobe Linearize a Pdf
        call: pdf-services-linearize-pdf.linearizepdf
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pdf-services-linearize-pdf-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adobe PDF Services API — Linearize PDF. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adobe-linearize-pdf
      description: Adobe Linearize a Pdf
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: pdf-services-linearize-pdf.linearizepdf
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
