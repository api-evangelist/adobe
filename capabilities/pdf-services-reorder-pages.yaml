naftiko: 1.0.0-alpha2
info:
  label: Adobe PDF Services API — Reorder Pages
  description: 'Adobe PDF Services API — Reorder Pages. 1 operations. Lead operation: Adobe Reorder Pages in a Pdf. Self-contained
    Naftiko capability covering one Adobe business surface.'
  tags:
  - Adobe
  - Reorder Pages
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADOBE_API_KEY: ADOBE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pdf-services-reorder-pages
    baseUri: https://pdf-services-ue1.adobe.io
    description: Adobe PDF Services API — Reorder Pages business capability. Self-contained, no shared references.
    resources:
    - name: operation-reorderpages
      path: /operation/reorderpages
      operations:
      - name: reorderpages
        method: POST
        description: Adobe Reorder Pages in a Pdf
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ADOBE_API_KEY}}'
  exposes:
  - type: rest
    namespace: pdf-services-reorder-pages-rest
    port: 8080
    description: REST adapter for Adobe PDF Services API — Reorder Pages. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/operation/reorderpages
      name: operation-reorderpages
      description: REST surface for operation-reorderpages.
      operations:
      - method: POST
        name: reorderpages
        description: Adobe Reorder Pages in a Pdf
        call: pdf-services-reorder-pages.reorderpages
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pdf-services-reorder-pages-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adobe PDF Services API — Reorder Pages. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adobe-reorder-pages-pdf
      description: Adobe Reorder Pages in a Pdf
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: pdf-services-reorder-pages.reorderpages
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
