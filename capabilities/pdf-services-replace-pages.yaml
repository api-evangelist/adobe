naftiko: 1.0.0-alpha2
info:
  label: Adobe PDF Services API — Replace Pages
  description: 'Adobe PDF Services API — Replace Pages. 1 operations. Lead operation: Adobe Replace Pages in a Pdf. Self-contained
    Naftiko capability covering one Adobe business surface.'
  tags:
  - Adobe
  - Replace Pages
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADOBE_API_KEY: ADOBE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pdf-services-replace-pages
    baseUri: https://pdf-services-ue1.adobe.io
    description: Adobe PDF Services API — Replace Pages business capability. Self-contained, no shared references.
    resources:
    - name: operation-replacepages
      path: /operation/replacepages
      operations:
      - name: replacepages
        method: POST
        description: Adobe Replace Pages in a Pdf
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ADOBE_API_KEY}}'
  exposes:
  - type: rest
    namespace: pdf-services-replace-pages-rest
    port: 8080
    description: REST adapter for Adobe PDF Services API — Replace Pages. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/operation/replacepages
      name: operation-replacepages
      description: REST surface for operation-replacepages.
      operations:
      - method: POST
        name: replacepages
        description: Adobe Replace Pages in a Pdf
        call: pdf-services-replace-pages.replacepages
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pdf-services-replace-pages-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adobe PDF Services API — Replace Pages. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adobe-replace-pages-pdf
      description: Adobe Replace Pages in a Pdf
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: pdf-services-replace-pages.replacepages
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
