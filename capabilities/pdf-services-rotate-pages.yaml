naftiko: 1.0.0-alpha2
info:
  label: Adobe PDF Services API — Rotate Pages
  description: 'Adobe PDF Services API — Rotate Pages. 1 operations. Lead operation: Adobe Rotate Pages in a Pdf. Self-contained
    Naftiko capability covering one Adobe business surface.'
  tags:
  - Adobe
  - Rotate Pages
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADOBE_API_KEY: ADOBE_API_KEY
capability:
  consumes:
  - type: http
    namespace: pdf-services-rotate-pages
    baseUri: https://pdf-services-ue1.adobe.io
    description: Adobe PDF Services API — Rotate Pages business capability. Self-contained, no shared references.
    resources:
    - name: operation-rotatepages
      path: /operation/rotatepages
      operations:
      - name: rotatepages
        method: POST
        description: Adobe Rotate Pages in a Pdf
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.ADOBE_API_KEY}}'
  exposes:
  - type: rest
    namespace: pdf-services-rotate-pages-rest
    port: 8080
    description: REST adapter for Adobe PDF Services API — Rotate Pages. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/operation/rotatepages
      name: operation-rotatepages
      description: REST surface for operation-rotatepages.
      operations:
      - method: POST
        name: rotatepages
        description: Adobe Rotate Pages in a Pdf
        call: pdf-services-rotate-pages.rotatepages
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pdf-services-rotate-pages-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adobe PDF Services API — Rotate Pages. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adobe-rotate-pages-pdf
      description: Adobe Rotate Pages in a Pdf
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: pdf-services-rotate-pages.rotatepages
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
